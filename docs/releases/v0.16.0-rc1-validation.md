# Validacion de GamuCare AI v0.16.0-rc1

## Ejecutado en el entorno de generacion

- 76 pruebas backend superadas.
- Cobertura global backend: 60 %.
- Compilacion completa de modulos Python correcta.
- `npm ci` completado.
- Compilacion React, TypeScript y Vite correcta.
- Ningun bloque JavaScript supera 500 kB.
- El nuevo metadato de version se comprueba mediante pruebas.

## Comandos ejecutados

```text
PYTHONPATH=. pytest -q --cov=app --cov-report=term --cov-report=json
python -m compileall -q app tests
npm ci --no-audit --no-fund
npm run build
```

## No ejecutado en este entorno

No hay motor Docker ni PowerShell disponibles. Debe verificarse en Docker Desktop:

1. `docker compose config --quiet`.
2. `upgrade-v0.16-rc1.ps1` sobre los volumenes reales.
3. `reset-demo.ps1` y reconstruccion completa de Qdrant.
4. `generate-evidence.ps1`.
5. Matriz de permisos.
6. Calidad de VetIA con retrieval y generacion.
7. Prometheus, Grafana, Alertmanager y Blackbox.
8. Backup, verificacion y restauracion en una copia.

## Criterio de promocion a v1.0.0

La version solo debe promocionarse cuando la lista
`docs/DELIVERY_CHECKLIST.md` este completada y no queden errores bloqueantes.
