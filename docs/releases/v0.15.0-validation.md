# Validacion de GamuCare AI v0.15.0

## Resultado ejecutado en el entorno de generacion

- 74 pruebas de backend superadas.
- Cobertura global del backend: 60 %.
- `app/core/errors.py`: 97 % de cobertura.
- Compilacion React, TypeScript y Vite correcta.
- Bundle dividido en cuatro grupos; ningun chunk supera 500 kB.
- Compilacion de modulos Python correcta.

## Pruebas especificas incorporadas

- Error 401 con `detail`, `error_code`, `request_id` y `X-Request-ID`.
- Error 422 sin reproducir el valor rechazado.
- Error 500 con mensaje generico y sin texto interno de la excepcion.
- Conservacion de la version 0.15.0 en health y evaluaciones.

## Comandos ejecutados

```text
PYTHONPATH=. pytest -q
PYTHONPATH=. pytest -q --cov=app --cov-report=term --cov-report=json
npm ci --no-audit --no-fund
npm run build
python -m compileall -q app tests
```

## Resultado no ejecutado en este entorno

No se ha levantado Docker Compose ni se dispone de PowerShell para ejecutar los
scripts de Windows. Deben verificarse en Docker Desktop:

1. `upgrade-v0.15.ps1` sobre los volumenes reales.
2. `test-permissions.ps1` con las credenciales locales.
3. `resilience-test.ps1` y recuperacion de Qdrant/Ollama.
4. `release-validation.ps1 -IncludeMonitoring`.
5. Backup, verificacion y, preferiblemente, restauracion en una copia.
6. Calidad de VetIA con retrieval y con generacion.
