# Validacion de GamuCare AI v0.14.0

## Resultado ejecutado en el entorno de generacion

- 71 pruebas de backend superadas.
- Cobertura global del backend: 59 %.
- Compilacion de todos los modulos Python modificados correcta.
- Compilacion React, TypeScript y Vite correcta.
- El build frontend genera una advertencia no bloqueante por un chunk superior a 500 kB.

## Casos especificos verificados

- G12 se clasifica como `out_of_scope` por cultura general.
- C07 se clasifica como `out_of_scope` por informatica, aunque contenga la palabra `historiales`.
- Las preguntas sobre pagos y planes dentro del alcance `pet` siguen aceptandose.
- Una referencia incidental a un ordenador no bloquea una urgencia veterinaria
  sobre un gato que ha mordido un cable.
- Las consultas fuera de dominio finalizan antes de solicitar el embedding.
- El emparejamiento por palabra completa evita interpretar `ue` dentro de `que`.
- Se mantiene la lectura de front matter Markdown.
- La extraccion HTML elimina `script` y `style` y conserva metadatos sidecar.
- El frontend compila con los nuevos campos de estado y diagnostico.

## Comandos ejecutados

```text
PYTHONPATH=. pytest -q
PYTHONPATH=. pytest -q --cov=app --cov-report=term-missing
npm ci
npm run build
```

## Resultado no ejecutado en este entorno

No se ha levantado Docker Compose completo ni se han descargado los documentos
externos desde los siete organismos. En Docker Desktop deben verificarse:

1. `upgrade-v0.14.ps1` con los volumenes reales.
2. `download-rag-sources.ps1 -Reindex`.
3. La nueva evaluacion desde Calidad de VetIA.
4. El recuento de documentos y vectores despues de indexar los PDF y HTML.
5. La evaluacion con generacion sobre Ollama.
